<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.road_friends.rentalcar.mapper.APIUserMapper">

    <!-- 로그인한 사용자 정보 조회 -->
    <select id="findById" resultType="com.road_friends.rentalcar.dto.UserDTO">
        SELECT user_num, user_id, user_name, user_email,
        user_phone, user_gender, user_birth, user_address,
        user_status, enabled
        FROM user WHERE user_id = #{userId}
    </select>

    <!-- 로그인한 사용자 정보 수정 -->
    <update id="update">
        UPDATE user
        SET user_name = #{userName}, user_email = #{userEmail},
        user_phone = #{userPhone} , user_address = #{userAddress}
        WHERE user_id = #{userId}
    </update>

    <!-- 로그인한 사용자 탈퇴 (DELETE 대신 enabled 값을 false 변경) -->
    <delete id="disable">
        UPDATE user SET enabled = false WHERE user_id = #{userId}
    </delete>


</mapper>

