<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.road_friends.rentalcar.mapper.PaymentMapper">
    <insert id="insertPayment" parameterType="com.road_friends.rentalcar.dto.PaymentDto">
        INSERT INTO payments (payment_id, payer_id, reservation_id, reservation_type, amount, currency,
            payment_gateway, state)
        VALUE (#{paymentId}, #{payerId}, #{reservationId}, #{reservationType},
            #{amount}, #{currency}, #{paymentGateway}, #{state});
    </insert>
</mapper>